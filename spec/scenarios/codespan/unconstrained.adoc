It will be ``npm install``ed (or ``yarn``ed) in the normal way.
Then just run `lerna`.
You can re``link`` all related packages using ``lerna``'s `link` command.
&quot;``foo`` `bar`&quot;
so `mono` man
